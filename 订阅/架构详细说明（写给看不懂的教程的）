架构详细说明
整体架构的核心本质，就是利用前置反代面板（1Panel/Nginx）处理对外的高风险操作（TLS 解密与 IP/域名接入），然后将 SubWAF 节点作为纯内部的明文 HTTP 订阅流量清洗中间件。

[ 客户端 ]
    │
 (公网 HTTPS 请求) ──> 支持 域名 或 HTTPS+IP（由 1Panel/Nginx 单独负责 TLS 证书解密）
        │
┌───▼───────────────────┐
│ 前置反代面板 (1Panel / Nginx)                │
└───┬────────────────┬──┘
        │                                │
 [仅订阅路径]                          [常规 API / 网页]
 /api/v1/client/subscribe              /api/v1/passthrough...
        │                                 │
        ▼                                 │
┌────────────┐               │
│ SubWAF 节点             │               │
│ (纯 HTTP: 127.0.0.2:8081) │              │
└───────┬─────┘               │
                │ (清洗通过)               │
                └──────┬──────┘
                              │
                              ▼
                ┌────────────┐
                │ 源站面板 (V2Board等)    │
                │ (纯 HTTP: 127.0.0.1:8080)│
                └────────────┘
关键技术细节厘清

节点绝不能暴露公网的原因： SubWAF Node 只支持明文 HTTP，本身不具备 TLS 证书解密和加密能力。如果将节点的端口直接暴露到公网，传输的所有订阅数据都是明文，极易被截获和篡改。因此必须依靠外层的 1Panel/Nginx 充当 TLS 前端（搞定 HTTPS 终结），Node 仅在本地内网（如 127.0.0.2）安全运行。
"HTTPS + IP"完全属于反代面板职责： 客户端是用域名访问，还是直接用 IP 加端口访问（如 [https://1.2.3.4:8443](https://1.2.3.4:8443)），完全由 1Panel/Nginx 单独配置，与 WAF 无关。SubWAF 节点只接收 Nginx 解密后发来的请求；只要节点后台"访问域名"填写 *（允许所有域名/IP）或正确透传 Host 头，节点就能无缝处理。
精准路径分流（只洗订阅流量）： 仅将高风险的订阅接口（如 /api/v1/client/subscribe）反代至 SubWAF 节点（127.0.0.2:8081）；其他如登录、封端客户端等常规 API，直接由 Nginx 转发给源站面板（127.0.0.1:8080），避开不必要的中间件损耗。
日志排查与精准风控（可选：订阅特化）

建议开启"订阅特化"： 如果需要更精准的异常流量排查与风险用户定位，请务必按照教程配置好「订阅特化 (subscription_specialization)」。
上报数据差异： 未配置该项时，SubWAF 日志仅能抓取并显示用户的 Token；配置完成后，系统会自动向后端上报机场用户的 UID / Email。
后期排查便利度： 仅凭 Token 排查风险用户时，往往还需要去源站数据库反查对应账号，用户刷新Token还会加大跟踪困难；开启订阅特化后，可以在 SubWAF 面板直接锁定恶意用户的具体邮箱并进行高效隔离，大幅提升风控处置效率。