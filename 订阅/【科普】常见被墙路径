机场节点被阻断的路径主要分为订阅墙（配置泄露引发的主动封禁）与流量墙（流量特征引发的被动封禁）两种逻辑：

1. 订阅墙（配置泄露 -> 审查方主动拉取 -> 批量阻断）
此路径的核心是节点信息被审查方直接获取，封禁与节点使用的传输协议无关。

泄露途径：

主动上报（内鬼行为）：恶意用户购买订阅后，直接将订阅链接提交给审查系统。

链路明文嗅探：用户在使用外部订阅转换等中间件时，若客户端与转换服务端之间采用未加密的 HTTP 明文传输，导致订阅内容在公网传输链路中被直接嗅探截获。另外， QQ 微信等国内大型社交软件 也应该视为明文传输。

多入口暴露：机场存在多个未做访问控制或审计的订阅拉取入口（例如未经审计的备用订阅域名）。

封端机场的客户端泄露：即使机场不直接提供订阅链接、仅允许用户通过闭源客户端连接，也不能排除配置泄露。因为现在 GFW 正逐步将这一过程自动化：审查方可直接获取并运行机场提供的闭源客户端（通常是 Android 或 Windows 平台），模拟正常用户登录和使用，从客户端中提取节点配置，再对节点实施批量阻断。

因此，「封端」并不等同于配置不会泄露。这种泄露导致的墙仍属于订阅墙。

这类泄露通常没有明显的订阅拉取入口或传统泄露痕迹，定位和复现都很困难。目前如果不能修改客户端，就没有特别有效的通用解决办法。

阻断逻辑：

审查系统获取订阅链接 -> 模拟正常客户端向服务器请求配置 -> 一次性解析出全部节点的 IP 与端口 -> 批量下发黑名单封锁。
典型特征：

速度快、范围大：拉取配置后整批节点瞬间瘫痪。

频次高：通常表现为单日被封 2 次及以上，无理论上限。若存在「一天 10 次以上、刚解析就秒墙」可以 100% 断定存在订阅泄露。

协议无关：无论采用何种加密或混淆协议，均无法防御。

2. 流量墙（数据交互 -> DPI 深度包检测 -> 渐进阻断）
此路径的核心是审查方在网络主干网被动监听，通过流量指纹与统计学行为识别代理通信。

协议演进与识别规律：

早期协议（Shadowsocks -> VMess）：依赖对称加密或早期混淆，缺乏现代标准 TLS 伪装，容易被纯统计学分析（包大小、熵值、握手时序）直接识别。

过渡协议（Trojan）：虽然引入了标准 TLS，但内部封装加密流量时会产生固定的"TLS in TLS"嵌套特征与握手行为偏差，已被 DPI 规则针对，容易触发流量墙。

新一代协议（anytls / Hysteria 2 等）：通过消除嵌套特征、优化传输层行为或基于 QUIC 协议重构指纹，大幅削弱了被动 DPI 的识别能力。

阻断逻辑：

主干网监听流量 -> 积累特定 IP/端口的通信样本 -> 命中指纹模型或异常阈值 -> 动态实施阻断或丢包。
典型特征：

滞后、渐进式：需要累积一定的并发和流量样本才会被识别。

频次低：只要不是早期协议，通常表现为一天 1~2 次左右或者更低。

协议强相关：越老旧的协议被封越快，迁移至新协议可显著降低触发频率。

VLESS
可以将其视为「过渡协议」和「新一代协议」的混合体，为避免不必要的麻烦，不推荐新机场使用。

配置脆弱易错：该协议并非开箱即用，极度依赖多项底层参数组合。漏配或错配任一开关都会直接失效，极易导致节点被封或者无法连接。
软件兼容性差：各类客户端的支持标准不统一，极易因版本不匹配出现报错或断连。
排错成本极高：排查故障极其繁琐，实际稳定性与抗封效果并不优于现有的成熟方案。
3. 封锁路径核心差异
判定维度	订阅墙（配置泄露）	流量墙（特征识别）
触发诱因	内鬼或其他类型的配置泄露	协议特征缺陷
封锁速度	极快（主动拉取后秒级阻断）	较慢（依赖流量样本累积）
发生频率	频繁（一天 >= 2 次，可达 10 次以上）	较低（一天 <= 2 次）
协议关联度	无关联（换任何协议都会被墙）	高度关联（新协议能有效抗封）
本站的效果	正确接入可有效降低被墙频率	没有效果，需要自行更换协议