---
description: 异常账号/流量排查时，IP 分析的边界（面板前有中转机器，连接节点的客户端 IP 不可信）。
alwaysApply: true
---

# 用户 IP 分析规则

## 背景

本站点的网络架构是：

```
客户端 → 中转机器 → 节点服务器
```

**节点服务器看到的"客户端 IP"实际是中转机的 IP，不是真实用户 IP。**

因此 `DeviceStateService` / Redis hash `user_devices:{userId}` 里记录的
`{nodeId}:{ip}` 中的 IP 全部是中转机出口 IP，**和用户是否异常无关**。

## 排查异常账号时必须遵守

1. **不要**把 Redis 设备列表里的 IP 当作"该用户的真实来源 IP"来判断共享/盗号。
2. **不要**因为同一账号出现多个机房 IP（AWS/GCP/Azure 等）就判定被转卖——
   这些很可能就是中转机本身就在机房里。
3. **不要**拿这些 IP 去做地理位置分析、机房识别、黑名单匹配。
4. **可以**用的异常信号：
   - 总流量数值本身（日流量暴涨、u/d 比例异常）
   - 订阅下载频率（Nginx 日志里订阅接口的拉取间隔，正常客户端很少 <5 分钟一次）
   - 订阅下载的 UA 和 IP（这一层是用户侧直连面板，**不经过中转**，IP 有意义）
   - 同一 token 被不同型号/品牌的客户端并发拉取（Shadowrocket + clash-verge + sing-box 同时出现 = 多客户端共用）
   - `online_count` 与套餐 `device_limit` 的关系
   - 登录日志、登录失败次数
5. 给出结论时，**区分"面板侧数据"和"节点侧数据"**：订阅下载日志来自面板侧（可信），
   节点上报的设备 IP 来自节点侧（经中转，不可信）。

## 总结

判定账号是否异常时，**只看面板侧信号 + 流量形态**，不要对连接节点的 IP 做任何
"真实用户画像"推断。
