{
    "name": "节点域名调度",
    "code": "bait_split",
    "version": "7.0.0",
    "description": "全量接管节点域名，支持用户池、凌晨收敛和单用户覆盖",
    "author": "XBoard Custom",
    "type": "feature",
    "require": {
        "xboard": ">=1.0.0"
    },
    "admin_menus": [
        {
            "id": "console",
            "title": "域名调度中心",
            "path": "console",
            "icon": "shield",
            "description": "可视化管理用户池、节点域名、筛查批次和用户规则"
        }
    ],
    "config": {
        "console_path": {
            "type": "string",
            "default": "/webadmin/plugins/bait-split/console",
            "label": "可视化控制台路径",
            "description": "登录管理后台后，在当前标签页打开或复制该路径"
        },
        "ip_webhook_secret": {
            "type": "string",
            "default": "",
            "label": "AWS IP 轮换接口密钥",
            "description": "至少 32 位随机字符串，用于校验 AWS 换 IP 脚本的 HMAC 签名"
        },
        "ip_rotate_api": {
            "type": "string",
            "default": "",
            "label": "换 IP 服务地址",
            "description": "外部换 IP 服务根地址，例如 http://95.40.222.102:12132。推送丢失时靠它主动对账当前地址，留空则只能等推送"
        },
        "ip_rotate_token": {
            "type": "string",
            "default": "",
            "label": "换 IP 服务 Token",
            "description": "调用 instance 与 replace-ip 接口的 Bearer Token"
        },
        "wall_lookback_seconds": {
            "type": "number",
            "default": 3600,
            "label": "被墙前回溯窗口（秒）",
            "description": "留档时统计死 IP 存活期内拉过订阅的用户，默认 3600"
        },
        "wall_fresh_max_seconds": {
            "type": "number",
            "default": 7200,
            "label": "IP 新鲜阈值（秒）",
            "description": "IP 存活超过此秒数才被墙，视为老 IP 首墙，曝光窗口不可信，只记事件不统计人，默认 7200"
        },
        "night_converge_enabled": {
            "type": "boolean",
            "default": false,
            "label": "凌晨收敛",
            "description": "墙压倒性落在凌晨且白天近乎为零，形态是批量主动探测；探测只找得到有流量的地址。开启后窗口内把普通用户挤到牺牲池，拿一两个 IP 换其余几个的存活。人工锁定的用户不参与"
        },
        "night_converge_pool_ids": {
            "type": "string",
            "default": "",
            "label": "牺牲池（逗号分隔）",
            "description": "窗口内承载用户的池，填接口标识或池 ID。限定只收自己人时，人落在自己归属的那个牺牲池，面板上挪了人夜里就跟着变；放开收全站时按用户 ID 在多个池之间定死分摊。留空则收敛不生效"
        },
        "night_converge_members_only": {
            "type": "boolean",
            "default": true,
            "label": "只收敛牺牲池自己的人",
            "description": "开启后只有本来就归属牺牲池的用户在窗口内被收敛，主组、安静组的人凌晨保持原 IP。关掉则全站凌晨都塞进牺牲池——那样牺牲池被墙也读不出是谁招的"
        },
        "night_converge_start": {
            "type": "number",
            "default": 1,
            "label": "收敛起始整点",
            "description": "含该点，默认 1"
        },
        "night_converge_end": {
            "type": "number",
            "default": 9,
            "label": "收敛结束整点",
            "description": "不含该点，默认 9。起始大于结束按跨零点处理"
        }
    }
}
