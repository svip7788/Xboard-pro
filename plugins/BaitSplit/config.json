{
    "name": "订阅诱饵分组",
    "code": "bait_split",
    "version": "2.1.0",
    "description": "为多个用户组分别执行多域名稳定分组，用于定位订阅泄露账号",
    "author": "XBoard Custom",
    "type": "feature",
    "require": {
        "xboard": ">=1.0.0"
    },
    "admin_menus": [
        {
            "id": "console",
            "title": "排查控制台",
            "path": "console",
            "icon": "shield",
            "description": "可视化配置分组、节点、域名和封锁结果"
        }
    ],
    "config": {
        "console_path": {
            "type": "string",
            "default": "/webadmin/plugins/bait-split/console",
            "label": "可视化控制台路径",
            "description": "登录管理后台后，在当前标签页打开或复制该路径"
        }
    }
}
