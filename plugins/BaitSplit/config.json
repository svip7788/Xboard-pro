{
    "name": "节点域名调度",
    "code": "bait_split",
    "version": "8.0.0",
    "description": "全量接管节点域名，支持用户池、树形排查和单用户覆盖",
    "author": "XBoard Custom",
    "type": "feature",
    "require": {
        "xboard": ">=1.0.0"
    },
    "admin_menus": [
        {
            "id": "console",
            "title": "域名调度中心",
            "path": "console",
            "icon": "shield",
            "description": "可视化管理用户池、节点域名、筛查批次和用户规则"
        }
    ],
    "config": {
        "console_path": {
            "type": "string",
            "default": "/webadmin/plugins/bait-split/console",
            "label": "可视化控制台路径",
            "description": "登录管理后台后，在当前标签页打开或复制该路径"
        },
        "ip_webhook_secret": {
            "type": "string",
            "default": "",
            "label": "AWS IP 轮换接口密钥",
            "description": "至少 32 位随机字符串，用于校验 AWS 换 IP 脚本的 HMAC 签名"
        },
        "ip_rotate_api": {
            "type": "string",
            "default": "",
            "label": "换 IP 服务地址",
            "description": "外部换 IP 服务根地址，例如 http://95.40.222.102:12132。推送丢失时靠它主动对账当前地址，留空则只能等推送"
        },
        "ip_rotate_token": {
            "type": "string",
            "default": "",
            "label": "换 IP 服务 Token",
            "description": "调用 instance 与 replace-ip 接口的 Bearer Token"
        }
    }
}
